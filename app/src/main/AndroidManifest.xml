<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zj.example.clip" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ClipToPaddingDemoActivity"/>
        <activity android:name=".ClipChildrenDemoActivity"/>
        <activity android:name=".ClipChildrenViewPagerDemoActivity"/>
        <activity android:theme="@android:style/Theme.Holo.Light.DarkActionBar" android:name=".ClipchildrenClippaddingDemo3Activity"/>
    </application>

</manifest>
